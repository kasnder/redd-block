// ESM entrypoint kept for runners that prefer an explicit module extension.
// The implementation remains in the `.js` file so the repository's e2e ESLint
// configuration checks it alongside the existing specs.
export * from './brave-system.js';
