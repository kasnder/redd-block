{
  "name": "redd-block",
  "version": "0.9.3",
  "description": "ReDD Block - block distracting websites and apps to stay focused",
  "scripts": {
    "dev": "npm run sync:helper && tauri dev",
    "dev:ios": "PATH=\"$(pwd)/scripts:$PATH\" APPLE_DEVELOPMENT_TEAM=JD647S9RT6 tauri ios dev --host",
    "sync:helper": "node -e \"require('child_process').execSync(process.platform === 'win32' ? 'powershell -ExecutionPolicy Bypass -File ./scripts/sync-helper.ps1' : './scripts/sync-helper.sh', {stdio: 'inherit'})\"",
    "build": "tauri build",
    "build:mac": "./scripts/build-mac.sh",
    "build:helper": "cd helper-daemon && cargo build --release",
    "build:helper:mac": "cd helper-daemon && cargo build --release && cp target/release/redd-block-helper target/release/redd-block-helper-aarch64-apple-darwin",
    "build:helper:win": "cd helper-daemon && cargo build --release && copy target\\release\\redd-block-helper.exe target\\release\\redd-block-helper-aarch64-pc-windows-msvc.exe",
    "build:ios": "PATH=\"$(pwd)/scripts:$PATH\" APPLE_DEVELOPMENT_TEAM=JD647S9RT6 npx tauri ios build && mkdir -p for-distribution && cp 'src-tauri/gen/apple/build/arm64/ReDD Block.ipa' 'for-distribution/ReDD Block.ipa' && echo '' && echo '✅ App Store IPA ready: for-distribution/ReDD Block.ipa' && echo '   Upload with Transporter or: xcrun altool --upload-app --type ios --file \"for-distribution/ReDD Block.ipa\" --apiKey \\$APPLE_API_KEY_ID --apiIssuer \\$APPLE_API_ISSUER'",
    "build:win": "powershell -ExecutionPolicy Bypass -File ./scripts/build-win.ps1",
    "test:helper": "node -e \"require('child_process').execSync(process.platform === 'win32' ? 'powershell -ExecutionPolicy Bypass -File ./scripts/test-helper-win.ps1' : 'sudo ./scripts/test-helper-mac.sh', {stdio: 'inherit'})\"",
    "tauri": "tauri",
    "vite:dev": "vite",
    "vite:build": "vite build",
    "vite:preview": "vite preview"
  },
  "keywords": [
    "blocker",
    "focus",
    "productivity",
    "tauri",
    "website-blocker",
    "app-blocker"
  ],
  "author": {
    "name": "The Reduce Digital Distraction Project",
    "email": "team@reddfocus.org"
  },
  "license": "CC-BY-NC-ND-3.0",
  "dependencies": {
    "@tauri-apps/api": "^2.9.1",
    "@tauri-apps/plugin-dialog": "^2.6.0",
    "@tauri-apps/plugin-fs": "^2.4.5",
    "@tauri-apps/plugin-shell": "^2.3.4",
    "emoji-picker-element": "^1.28.1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.9.6",
    "dotenv": "^17.2.3",
    "sharp": "^0.34.5",
    "vite": "^7.3.1"
  }
}